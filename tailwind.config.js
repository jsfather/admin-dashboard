const colors = require('tailwindcss/colors')

export const theme = {
    colors: {
        primary: colors.blue["600"],
        secondary: colors.sky["400"],
        base: colors.white,
        opposite: colors.neutral["800"],
        background: colors.gray["100"]
    }
};